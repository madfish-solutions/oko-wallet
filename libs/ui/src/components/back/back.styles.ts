import { StyleSheet } from 'react-native';

export const BackStyles = StyleSheet.create({
  root: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    padding: 4
  }
});
