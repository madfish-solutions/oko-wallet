export const modalScreenOptions = {};
export const modalScreenOptionsWithBackButton = {};

// align web extension
const extensionHeight = 600;
const extensionTopOffset = (window.innerHeight - extensionHeight) / 2;
export const alignCenterExtension = { marginTop: extensionTopOffset };
