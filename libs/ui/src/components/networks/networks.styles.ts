import { StyleSheet } from 'react-native';

export const NetworksStyles = StyleSheet.create({
  balanceWrapper: {
    marginBottom: 8
  },
  balance: {
    fontWeight: '700'
  },
  network: {
    marginBottom: 4
  }
});
