import { StyleSheet } from 'react-native';

export const NetworksStyles = StyleSheet.create({
  balanceWrapper: {
    marginBottom: 8,
    color: '#000'
  },
  balance: {
    fontWeight: '700'
  },
  network: {
    marginBottom: 4,
    color: '#000'
  },
  networkName: {
    color: '#000'
  }
});
