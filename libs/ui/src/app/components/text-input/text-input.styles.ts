import { StyleSheet } from "react-native";

export const StyledTextInputStyles = StyleSheet.create({
  root: {
    paddingLeft: 12,
    paddingRight: 12,
    borderWidth: 1,
    borderStyle: 'solid',
    borderColor: "#dcdcdc",
    borderRadius: 4,
    backgroundColor: '#fff',
  },
});
