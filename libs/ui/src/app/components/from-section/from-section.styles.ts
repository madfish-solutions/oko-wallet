import { StyleSheet } from "react-native";

export const SectionStyles = StyleSheet.create({
  root: {
    display: 'flex',
    flexDirection: 'column',
  },
  input: {
    height: 38,
  },
  error: {
    marginTop: 8,
    color: 'red',
  }
});
