import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  root: {
    padding: 20,
    marginBottom: 100
  },
  input: {
    height: 40,
    margin: 12,
    borderWidth: 1,
    padding: 10
  }
});
