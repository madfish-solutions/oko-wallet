import { StyleSheet } from 'react-native';

export const CollectiblesStyles = StyleSheet.create({
  wrapper: {
    marginBottom: 24,
    color: '#000'
  },
  boldText: {
    fontWeight: '700'
  },
  image: {
    width: 40,
    height: 40
  }
});
