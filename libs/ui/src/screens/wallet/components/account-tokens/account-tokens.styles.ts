import { StyleSheet } from 'react-native';

export const AccountTokensStyles = StyleSheet.create({
  root: {
    padding: 12
  },
  input: {
    height: 40,
    margin: 12,
    borderWidth: 1,
    padding: 10
  }
});
