import { StyleSheet } from 'react-native';

export const WalletStyles = StyleSheet.create({
  root: {
    padding: 12
  },
  wrapper: {
    marginBottom: 24,
    color: '#000'
  },
  boldText: {
    fontWeight: '700'
  }
});
