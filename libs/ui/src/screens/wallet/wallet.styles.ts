import { StyleSheet } from 'react-native';

export const WalletStyles = StyleSheet.create({
  root: {
    padding: 12
  },
  balanceWrapper: {
    marginBottom: 24
  },
  boldText: {
    fontWeight: '700'
  }
});
