export interface DAppInfo {
  origin: string;
  name: string;
  favicon: string;
}

export interface DAppSendTransactionInfo {
  data: string;
  from: string;
  gas: string;
  gasPrice: string;
  to: string;
  value: string;
}
